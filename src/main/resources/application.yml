server:
  port: 8080

spring:
  application:
    name: admin
  profiles:
    active: local
  config:
    import:
      - optional:configserver:http://54.180.92.188:8888/
logging:
  level:
    root: info

